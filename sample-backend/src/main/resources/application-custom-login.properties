# =====================================================
# Custom Login Mode Configuration
# Enable this profile to use custom login (username/password)
# =====================================================

# Enable Custom Login - User enters credentials in YOUR frontend
oauth2.custom-login-enabled=true

# This mode uses Resource Owner Password Credentials Grant
# User credentials are sent directly to your backend
# Backend exchanges them for tokens with the OAuth2 provider
